import { InternalRegisterRequest } from './internal-register-request';

describe('InternalRegisterRequest', () => {
  it('should create an instance', () => {
    expect(new InternalRegisterRequest()).toBeTruthy();
  });
});
