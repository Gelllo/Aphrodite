import { UpdateUserProfileRequest } from './update-user-profile-request';

describe('UpdateUserProfileRequest', () => {
  it('should create an instance', () => {
    expect(new UpdateUserProfileRequest()).toBeTruthy();
  });
});
